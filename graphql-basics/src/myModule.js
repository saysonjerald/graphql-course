const meesage = "Hello myname";
const name = "Jerald Sayson";
const address = "Philippnies";

const getGreeting = (name) => {
    return `Hello ${name}, Welcome the club!`
}

export {meesage, name, getGreeting, address as default}